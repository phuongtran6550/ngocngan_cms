# Phoenix asset source

The theme stylesheet and selected image assets in this directory come from the locally provided Phoenix v1.24.0 reference under `Template2/public/reference/source/assets`.

Only assets required by the shared CMS shell and the bundled Phoenix stylesheet are copied. The application does not read from `Template2` at runtime.
